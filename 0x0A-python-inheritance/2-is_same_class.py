#!/usr/bin/python3

"""
class declaration
"""


def is_same_class(obj, a_class):
    """
    checks the data type
    """
    if type(obj) is a_class:
        return True
    else:
        return False
